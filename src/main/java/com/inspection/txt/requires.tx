
需求：
1、巡检项按组展示
2、支持分布式、多实例
3、支持启动、暂停、继续、终止
4、支持勾选部分巡检项
5、支持前端自动展示
6、支持查询总体进度
7、支持对单个巡检项设置超时时间

扩展能力
- 支持kafka下发和上报接口
- 支持redis下发和上报接口

高可靠
- 启动巡检任务之后，如果巡检服务异常，重启之后要保持推出前的状态，状态为stopped ==> 任务持久化，优先redis、PG、文件

